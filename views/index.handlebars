<h1>Travel Bucketlist</h1>
<h2>Add a destination of where you have been or want to go!</h2>

<h2>Places I've Been</h2>

<ul>
  {{#each city}}
    {{#unless place}}
      {{> city/cit-block place=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Places I Want to Go</h2>

<ul>
  {{#each city}}
    {{#if place}}
      {{> city/cit-block place=false}}
    {{/if}}
  {{/each}}
</ul>


<h2>Add a City</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">City Name:</label>
    <input type="text" id="city" name="name">
  </div>

  <div class="form-group">
    <label for="destination"></label><br>
    <input type="radio" name="place" value="1" checked> Been there!<br>
    <input type="radio" name="place" value="0"> Want to go
  </div>

  <button type="submit">Add City</button>
</form>
